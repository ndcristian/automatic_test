{
    "scripts": {
        "ng": "ng",
        "start": "angular-http-server --path ./dist/test -p 4200",
        "build": "ng build",
        "watch": "ng build --watch --configuration development",
        "test": "ng test"
      },
    "devDependencies": {
        "cypress": "^8.4.1"
    }
}
