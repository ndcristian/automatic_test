{
    "scripts": {
        "ng": "ng",
        "start": "angular-http-server --path ./dist/test -p 4200 & npx cypress run --spec cypress/integration/alg/model/*.spec.js --headless ",
        "build": "ng build",
        "watch": "ng build --watch --configuration development",
        "test": "npx cypress run --spec cypress/integration/alg/model/*.spec.js --headless "
      },
    "devDependencies": {
        "cypress": "^8.4.1" 
    }
}
